---
const { heroBackground } = Astro.props;
---

<div
  id="hero"
  class="relative h-screen w-screen bg-center bg-cover overflow-x-hidden"
  style={`background-image: url('${heroBackground}');`}
>
  <slot />
</div>
